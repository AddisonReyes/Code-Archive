// src/app/dashboard/page.js (route: /dashboard)
export default function DashboardPage() {
  return (
    <div>
      <h1>User Dashboard</h1>
      <p>
        This page is wrapped by both the RootLayout and the DashboardLayout.
      </p>
    </div>
  );
}

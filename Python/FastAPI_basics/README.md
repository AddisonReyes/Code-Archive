# FastAPI Basics

In this project, we will be building a simple API using FastAPI. FastAPI is a modern, fast (high-performance) web framework for building APIs with Python 3.6+ based on standard Python type hints.

To run the application, use the following command in your terminal:

```bash
uv run fastapi dev main.py
```

##

Made without AI tools.
